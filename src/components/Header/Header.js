import React from "react";
import NavBar from "../NavBar/NavBar";

const Header = () => {
  return (
    <React.Fragment>
      <nav className="mx-2">
        <NavBar />
      </nav>
    </React.Fragment>
  );
};

export default Header;
