import React from "react";
import "./Profile.css";

const Profile = () => (
  <div className="Profile">
    <h1>Profile Component</h1>
    <p>
      NOTE: Nothing was required for this exercise to fully implement this item,
      the home page and reservations section are the core requirements. As time
      allows, I may continue to develop this site.
    </p>
  </div>
);

Profile.propTypes = {};

Profile.defaultProps = {};

export default Profile;
